server:
  port: 8081 # 원하는 포트 번호로 변경
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasorce:
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true